import { FilterByDepartamentoLocalidadPipe } from './filter-by-departamento-localidad.pipe';

describe('FilterByDepartamentoLocalidadPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByDepartamentoLocalidadPipe();
    expect(pipe).toBeTruthy();
  });
});
