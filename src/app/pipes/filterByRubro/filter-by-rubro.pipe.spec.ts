import { FilterByRubroPipe } from './filter-by-rubro.pipe';

describe('FilterByRubroPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByRubroPipe();
    expect(pipe).toBeTruthy();
  });
});
