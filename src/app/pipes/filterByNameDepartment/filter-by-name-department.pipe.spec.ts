import { FilterByNameDepartmentPipe } from './filter-by-name-department.pipe';

describe('FilterByNameDepartmentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNameDepartmentPipe();
    expect(pipe).toBeTruthy();
  });
});
 